import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#FFC0CB',
    },
    secondary: {
      main: '#5342fc',
    },
    otherColor: {
      main: '#AAB435',
    },
  },
});

export { theme };
